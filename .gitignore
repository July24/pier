node_modules/
# macOS Finder artifacts（本机全局 ignore 已覆盖；入库此规则保证 CI/新克隆干净）
.DS_Store
packages/pier-workbench/scripts/boot-config.json
# 本仓库的 CI workflow 需要入库（全局 gitignore 忽略了 .github/，此处显式例外）
!.github/
!.github/**
docs/research/
reports/
.stryker-tmp/
# /pier-config doc output (machine-truth report; regenerate on demand)
.pi-herdr/config-report.md

# Development-only docs (reviews, RFCs, per-feature design notes) are local artifacts:
# the repository tracks user-facing documentation and code only. Remove a line here if a
# document becomes user-facing.
code review.md
docs/rfc-*.md
docs/pier-config-command.md
docs/adr/
docs/history/
docs/efficiency-review-*.md
docs/herdr-0.9.1-optimization-design.md
spike/
